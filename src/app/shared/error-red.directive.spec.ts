import { ErrorRedDirective } from './error-red.directive';

describe('ErrorRedDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorRedDirective();
    expect(directive).toBeTruthy();
  });
});
